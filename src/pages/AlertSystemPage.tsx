import React from 'react';
import AlertSystemApp from '@/alert-system/App'; // Import the Alert System App component

const AlertSystemPage = () => {
  return (
    // Removed the div wrapper as the AlertSystemApp likely provides its own layout
    <AlertSystemApp />
  );
};

export default AlertSystemPage;
